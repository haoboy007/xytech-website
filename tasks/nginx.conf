server {
    listen 80;
    server_name www.cnxy.tech cnxy.tech;
    index index.html;
    root /www/wwwroot/xytech;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1000;

    # 静态资源缓存（30天）
    location ~* \.(js|css|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot|ico)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 核心：React SPA 路由回退
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 安全响应头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# HTTPS 配置（申请 SSL 证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name www.cnxy.tech;
#     index index.html;
#     root /www/wwwroot/xytech;
#
#     ssl_certificate /www/server/panel/vhost/cert/www.cnxy.tech/fullchain.pem;
#     ssl_certificate_key /www/server/panel/vhost/cert/www.cnxy.tech/privkey.pem;
#
#     gzip on;
#     gzip_vary on;
#     gzip_types text/plain text/css application/javascript application/json image/svg+xml;
#     gzip_min_length 1000;
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot|ico)$ {
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
# }
#
# # HTTP 重定向到 HTTPS
# server {
#     listen 80;
#     server_name www.cnxy.tech cnxy.tech;
#     return 301 https://$server_name$request_uri;
# }
